# 💎 GEM OS

Um assistente de voz modular com suporte a STT, TTS, LLMs e plugins extensíveis.

## 🚀 Como rodar

```bash
# inicialização normal
python gem.py

# com debug
python gem.py --debug

# modo dry-run (carrega sistemas sem entrar no loop principal)
python gem.py --dry-run
