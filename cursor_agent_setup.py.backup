#!/usr/bin/env python3
"""
🔥 CURSOR AGENT SETUP - GEM OS PROJECT
Setup Cursor Agent for Linear coordination and AI team management
"""

import subprocess
import os

def setup_cursor_agent():
    """Setup Cursor Agent for GEM OS project"""
    print("🔥 CURSOR AGENT SETUP - GEM OS PROJECT")
    print("=" * 50)
    
    # Change to project directory
    os.chdir("/home/oem/PycharmProjects/gem")
    
    print("✅ Cursor Agent installed: 2025.08.27-24c29c1")
    print("✅ PATH configured with ~/.local/bin")
    
    print("\n🎯 CURSOR AGENT TASKS FOR GEM OS:")
    print("1. 📋 Create Linear issues for AI team")
    print("2. 👥 Coordinate AI team assignments")
    print("3. 📊 Monitor project progress")
    print("4. 🔒 Implement security features")
    print("5. 🚀 Manage 20-day development timeline")
    
    print("\n💻 CURSOR AGENT COMMANDS:")
    print("cursor-agent --help                    # Show help")
    print("cursor-agent --project /home/oem/PycharmProjects/gem  # Set project")
    print("cursor-agent --task 'Linear integration'  # Run task")
    
    print("\n🔑 CURSOR API KEYS READY:")
    print("GEMOSLINEAR: key_5e39...a283")
    print("GEMOS: key_b84f...a7d1")
    
    print("\n🚀 READY TO START CURSOR AGENT!")

def run_cursor_agent_for_gem():
    """Run Cursor Agent for GEM OS project"""
    print("\n🚀 STARTING CURSOR AGENT FOR GEM OS...")
    
    # Start Cursor Agent with GEM OS project
    try:
        subprocess.run([
            "cursor-agent", 
            "--project", "/home/oem/PycharmProjects/gem",
            "--task", "Coordinate GEM OS development with Linear integration"
        ])
    except Exception as e:
        print(f"❌ Error starting Cursor Agent: {e}")
        print("💡 Try running manually: cursor-agent")

if __name__ == "__main__":
    setup_cursor_agent()
    
    start_agent = input("\n🚀 Start Cursor Agent now? (y/n): ").strip().lower()
    if start_agent in ['y', 'yes']:
        run_cursor_agent_for_gem()
    else:
        print("🎯 Run manually when ready: cursor-agent")